import Button from '@/components/Button/Button';

export default function Home() {
  return (
    <main>
      <Button className="large_active" disabled={true}>
        로그인
      </Button>
    </main>
  );
}
